window.onload=function(){
   /* 
    var Wscreen = window.document.body.clientWidth;
    var urls=document.getElementById("navUl");
    var menu=document.getElementById("menu");
    var copy;
    if(Wscreen <= 767){
    /*copy=urls.innerHTML;
    urls.innerHTML="";
    urls.style.display="none";
    menu.innerHTML += copy;
    }
    for(i=0 ; urls.getElementsByTagName("li").length; i++){
        
        copy=urls.getElementsByTagName("li")[i];
        console.log(copy)
        urls.removeChild(urls.childNodes[i]);
        console.log(urls.getElementsByTagName("li").length);
        copy.className += "place-swich";
        menu.appendChild(copy);
        
    }
    
    }*/
};